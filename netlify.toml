[build]
  command = "npm run build"
  publish = "dist"

# GA4 Measurement ID: set PUBLIC_GA_MEASUREMENT_ID in Site settings >
# Environment variables. If unset, Google Analytics stays disabled
# (see the GA_ID_VALID check in src/scripts/consent.js).
